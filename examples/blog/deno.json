{
  "tasks": {
    "start": "deno run -A --watch=. server.ts",
    "generate": "deno eval \"import { generateFiles } from 'mastro/generate.ts'; generateFiles()\""
  },
  "patch": ["../../"],
  "imports": {
    "mastro/": "../../src/"
  }
}
