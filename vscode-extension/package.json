{
  "name": "mastro-ssg-vscode-extension",
  "displayName": "Mastro SSG (static site generation)",
  "description": "Build your static site inside your browser using vscode.dev or github.dev",
  "version": "0.0.1",
  "repository": {
    "url": "https://github.com/mb21/mastro.git",
    "type": "git"
  },
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "browser": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mastro.start",
        "title": "Start dev server",
        "category": "mastro"
      }
    ]
  },
  "scripts": {
    "open-in-browser": "vscode-test-web --extensionDevelopmentPath=. ../examples/hello-world/",
    "vscode:prepublish": "npm run build",
    "build": "tsc -p ./ && tsc ../src/*.ts --outDir mastro --target esnext --rewriteRelativeImportExtensions",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.96.0",
    "@vscode/test-web": "*",
    "typescript": "^5.7.2"
  },
  "volta": {
    "node": "23.5.0"
  }
}
